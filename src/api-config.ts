import { PRODUCTION_API_BASE } from "./deploy-urls";

/** Default NestJS API origin when no build-time env is set (no trailing slash). */
export const DEFAULT_PRODUCTION_API_BASE = PRODUCTION_API_BASE;
