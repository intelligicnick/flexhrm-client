# Copy to .env.production before `npm run build` for static Hostinger deploys.
# The build writes dist/runtime-config.js so the browser calls your NestJS API directly.
PUBLIC_API_URL=https://steelblue-hamster-881717.hostingersite.com
