This folder has been superseded by the full Android project at `../android-supervisor-app/`.

The `FlexHrmAndroidBridge.java` source now lives in:
`android-supervisor-app/app/src/main/java/com/flexhrm/supervisor/FlexHrmAndroidBridge.java`
